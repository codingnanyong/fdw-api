{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "Productions": [
    "http://{url}/{path}/{query}",
    "http://{url}/{path}/{query}",
    "http://{url}/{path}/{query}"
  ],
  "IoTs": [
    "http://{url}/{path}/{query}",
    "http://{url}/{path}/{query}",
    "http://{url}/{path}/{query}"
  ]
}